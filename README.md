# webApps

Repository for web applications.

## GitHub Pages

This repository deploys the `site/` directory to GitHub Pages with
`.github/workflows/deploy-pages.yml`. The published landing page is the
project site root,
`https://jmattst1.github.io/webApps/`.
