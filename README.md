# webApps

Repository for web applications.

## GitHub Pages

This repository now publishes the music practice tracker mockup from `/index.html` through the GitHub Pages deployment workflow at `/.github/workflows/deploy-pages.yml`.
